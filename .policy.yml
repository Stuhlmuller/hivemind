policy:
  approval:
    - signed

approval_rules:
  - name: signed
    requires:
      conditions:
        has_valid_signatures: true
  - name: cursor bot approved
    requires:
      conditions:
        count: 1
        users:
          - "chatgpt-codex-connector[bot]"
