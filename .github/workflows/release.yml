name: "Release"

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  release:
    uses: Stuhlmuller/workflows/.github/workflows/release.yml@38705c434ffc5271e58b83b95399a8ea047f578d # main
