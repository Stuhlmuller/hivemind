name: "Release"

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  release:
    uses: Stuhlmuller/workflows/.github/workflows/release.yml@9404d2a4f865af2d7241c5722b80beb837fbd600 # main
