# Hivemind Developer Loop

This compatibility prompt now defers to the worker lane wording. Keep the worker prompt as the source of truth.

Use `.agents/PROMPT-worker.md` instead of relying on this file directly.
