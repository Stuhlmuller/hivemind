interface:
  display_name: "Review Follow-up"
  short_description: "Address and resolve PR feedback"
  default_prompt: "Use $gh-address-comments to review unresolved PR feedback, implement selected fixes, push when requested, resolve addressed review threads after the fix is pushed, and summarize the result."
